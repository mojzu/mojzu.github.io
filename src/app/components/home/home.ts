
export interface ITile {
  background: string;
  link: string;
  title: string;
  description: string;
}

export interface IRouteData {
  title: string;
}

export const homeRouteData: IRouteData = {
  title: "Home",
};
