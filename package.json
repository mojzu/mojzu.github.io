{
  "name": "mojzu.net",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "sapper dev",
    "build": "sapper build",
    "start": "node build",
    "export": "rm -rf docs && sapper export && mv export docs",
    "serve": "serve docs"
  },
  "dependencies": {
    "compression": "^1.7.2",
    "marked": "^0.4.0",
    "polka": "^0.4.0",
    "sapper": "^0.14.1",
    "sirv": "^0.1.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "serve": "^9.2.0",
    "svelte": "^2.9.1",
    "svelte-loader": "^2.9.1",
    "webpack": "^4.15.1"
  }
}
