# mojzu.net

## Developer

```Shell
# Install dependencies.
$ yarn
# Run development server (localhost:3000).
$ yarn run dev
# Build for deployment.
$ yarn run build && yarn start
# Export for static deployment.
$ yarn run export && yarn run serve
```
